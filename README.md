# bisectrunk-rlang

Use `bisectrunk` to pinpoint the exact commit introduced in rlang that
caused the downstream error described in
[keaven/gsDesign#283](https://github.com/keaven/gsDesign/pull/283).

Commit range is from `v1.1.7` (`7a519a2`) to current HEAD (`b7b9861`),
188 commits in total.
